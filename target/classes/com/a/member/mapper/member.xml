<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
	<!-- 회원가입 추가 정보 -->
 	<insert id="memberInfoPro" parameterType="com.a.dto.MemberDto">
		INSERT INTO MEMBER (MEMBERSEQ, EMAIL, NICKNAME, MEMBERPHOTO, NAVERLOGIN, AUTH, MEMBERDEL) 
		VALUES (MEM_SEQ.NEXTVAL, #{email}, #{nickname}, 0, #{naverLogin}, 3, 0)
	</insert>
	
	<!-- 닉네임 중복 확인 -->
	<select id="nickCheck" parameterType="com.a.dto.MemberDto" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM MEMBER WHERE NICKNAME = #{nickname}
	</select>
	
	<!-- 네이버 이메일 체크 후 정보 가져감 -->
	<select id="naverConnectionCheck" resultType="java.util.Map" parameterType="java.util.Map">
		SELECT EMAIL,NICKNAME,NAVERLOGIN FROM MEMBER 
		WHERE EMAIL = #{email}
	</select>
	
	<!-- 네이버 로그인 -->
	<update id="setNaverConnection" parameterType="java.util.Map">
		UPDATE MEMBER SET NAVERLOGIN = #{id}
		WHERE EMAIL = #{email}
	</update>
	
	<insert id="memberNaverRegisterPro" parameterType="java.util.Map">
		INSERT INTO MEMBER (MEMBERSEQ,EMAIL,NICKNAME,NAVERLOGIN) 
		VALUES (MEM_SEQ.NEXTVAL,#{email},#{nickname},#{naverLogin})
	</insert>
	
	<!-- id에 원래 naverLogin -->
	<select id="memberNaverLoginPro" resultType="java.util.Map" parameterType="java.util.Map">
		SELECT EMAIL,NICKNAME FROM MEMBER 
		WHERE EMAIL = #{email} AND NAVERLOGIN = #{id}
	</select>
	
	<!-- 여기서부터 카카오 -->
	<!-- 카카오 이메일 체크 -->
	<select id="kakaoConnectionCheck" resultType="java.util.Map" parameterType="java.util.Map">
		SELECT EMAIL,NICKNAME,KAKAOLOGIN FROM MEMBER 
		WHERE EMAIL = #{email}
	</select>
	
	<update id="setKakaoConnection" parameterType="java.util.Map">
		UPDATE MEMBER SET KAKAOLOGIN = #{id}
		WHERE EMAIL = #{email}
	</update>
	
	<select id="memberKakaoLoginPro" resultType="java.util.Map" parameterType="java.util.Map">
		SELECT EMAIL,NICKNAME FROM MEMBER 
		WHERE EMAIL = #{email} AND KAKAOLOGIN = #{kakaoLogin}
	</select>
	
	<insert id="memberKakaoRegisterPro" parameterType="java.util.Map">
		INSERT INTO MEMBER (MEMBERSEQ,EMAIL,NICKNAME,KAKAOLOGIN) 
		VALUES  (MEM_SEQ.NEXTVAL,#{email},#{nickname},#{kakaoLogin})
	</insert>
	
</mapper>