DROP TABLE APPLY
CASCADE CONSTRAINTS;

--APPLY

CREATE TABLE APPLY(
		ID VARCHAR2(20),
		PARTNERID VARCHAR2(20),
		HOBBY VARCHAR2(20),
		AREA VARCHAR2(100),
  		CONSTRAINT FK_APP FOREIGN KEY (ID) 
        REFERENCES MEMBER(ID),
        CONSTRAINT FK_PAPP FOREIGN KEY (PARTNERID) 
        REFERENCES MEMBER(ID)
)

 