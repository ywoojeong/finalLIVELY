DROP TABLE APPLYBBS
CASCADE CONSTRAINTS;

DROP SEQUENCE APP_SEQ;

--APPLYBBS

CREATE TABLE APPLYBBS(
		APPLYBBSSEQ NUMBER CONSTRAINT APP_PRI PRIMARY KEY,
		APPLYBBSTITLE VARCHAR2(200),
		APPLYBBSCONTENT VARCHAR2(4000),
		APPLYBBSDATE DATE,
		APPLYBBSDEL NUMBER,
		
		LIKESEQ NUMBER,
		EMAIL VARCHAR2(50),
		
  		CONSTRAINT FK_APP FOREIGN KEY (LIKESEQ) 
        REFERENCES MYLIKE(LIKESEQ),
        CONSTRAINT FK_APP2 FOREIGN KEY (EMAIL) 
        REFERENCES MEMBER(EMAIL)
)


CREATE SEQUENCE APP_SEQ
START WITH 10000
INCREMENT BY 1

SELECT * FROM APPLYBBS

 